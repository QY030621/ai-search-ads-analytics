# Phase 2 验收记录

- 30 项 pytest 测试全部通过（2026-09-07）。
- 覆盖 168 行数据结构、四种情景手算、先汇总后计算、三级汇总一致性、零分母、日期/Campaign 筛选、缺失日期、非法数值和 CSV 结构。
- Streamlit AppTest 覆盖正常启动、筛选、空结果、错误文件阻止计算、上传零转化文件显示 CPA=N/A 和 ROAS=0、缺失日期提示。
- 本机实际服务已启动；/_stcore/health 返回 HTTP 200 / ok；浏览器显示真实 KPI、Plotly 趋势及两级表格。
- 未实现诊断规则或 AI Recommendations；停在 Phase 2。

## Phase 3 验收

- 57 项测试通过（含 Phase 2 的 30 项和 Phase 3 的 27 项）；最终运行：python -m pytest -q -p no:cacheprovider，57 passed，未产生警告。
- 覆盖四类规则、阈值相等与略低、CPA严格大于目标、正常目标边界、双指标共同变化、样本不足、零基准、缺失日期/整条Ad、当前窗口独立零转化判断、完整回传声明、筛选及真实Streamlit组件渲染。
- 实际浏览器已加载 Diagnostics 区域，9个对象结果与模拟情景一致。每个对象有双窗口证据和四条规则的评估说明。
- 停在 Phase 3；没有 AI Recommendations、暂停广告或预算调整建议。

## Phase 4 验收与限制

- 78 项测试通过（含既有57项及新增21项）；测试包括结构化输入隔离、六段输出、证据逐字保留、三类不同建议、正常/不足不调用模型、API成功模拟、HTTP请求契约、超时/拒绝/不完整/非法内容降级及筛选后旧结果失效。
- 真实浏览器在 http://127.0.0.1:8502/ 已显示建议区域，展开CTR异常可见完整六段内容及“规则备用建议（非 AI 生成）”标识。
- 原8501服务缓存旧模块且无法终止；新版服务使用8502，启动配置和README已同步。
- 未配置API密钥或模型，未做真实远程AI调用。已完成本地功能和离线/模拟接口验收；真实API连通性及实际模型选择质量仍待配置验证。
- 无UI精修、额外业务功能或作品集制作。

## 最终审查修复验收（2026-09-08）

- 90 项测试通过，新增覆盖购买金额矛盾、计算结果引用、空窗与真实零值区分、部分 Campaign 日期趋势断开、原因与实验对应、本地配置、上传拦截及缺失窗口组件显示。
- 真实浏览器验证：矛盾 CSV 阻止指标计算；缺失 7 条记录的 CSV 显示覆盖警告与趋势断开说明。8502 页面正常运行。
- 真实 API 验证脚本已准备，逐类检查 CTR 下滑、CPC/CPA 恶化、零转化的真实返回；fallback 不计通过。当前缺少密钥与模型配置，脚本已确认停止且未发送请求。真实调用尚未验收，不将离线测试称为真实 AI 成功。
- 本轮只修改既有校验、缺失显示、建议对应关系及 API 配置验证，不进行 UI 美化或新增业务功能。

## DeepSeek 自定义服务商验证

- 使用 OpenAI Python SDK 连接 https://api.deepseek.com，模型 deepseek-v4-flash；支持 AI_API_KEY、AI_BASE_URL、AI_MODEL，保留原有 JSON Schema、数字证据与 fallback。
- DeepSeek 默认思考模式曾耗尽输出预算；仅对该官方地址设置 reasoning.effort=none，并补充结果对象说明。没有放宽校验或把错误结果转成成功。
- 真实请求中三类异常均曾产生合法返回，但输出并不稳定：最终一轮 CTR 下滑未通过事件结构校验，CPC/CPA 恶化与零转化通过。完整三类稳定验收未通过，不把较早成功结果拼接为本轮全部成功。
- 观察到服务商返回 JSON Schema 定义本身而非结果对象，已增加该失败回归场景。实际失败均保留规则备用建议和原诊断，数字证据未改变。
- 最终一轮详细结果见 LIVE_AI_VALIDATION.json；密钥未写入代码、配置模板或报告。后续使用需在本机私有配置填入密钥。

## CTR 兼容修复后的最终验收（2026-09-09）

- 原失败返回是 JSON Schema 定义，顶层为 type/properties/required/additionalProperties，而非必需的 event_0；不是数字或合法候选数量被过严限制。相同 Schema 也曾直接通过，证明返回存在不稳定。
- 仅当 DeepSeek 请求包含 CTR 异常且返回与本次 Schema 完全相同时，追加一次格式纠正请求。Schema、诊断输入、合法 ID、固定六段建议与数字保护均不变。再次失败仍 fallback；不修补或接受错误字段。
- 修复后先单独真实验证 CTR：通过，证据未改动。随后只执行一轮三类最终真实验收：CTR 下滑、CPC/CPA 恶化、零转化全部通过，三类观察证据全部保持原样。详细结果已更新至 LIVE_AI_VALIDATION.json。
- 100 项回归测试通过，覆盖格式纠正成功、连续 Schema 回显后的 fallback、伪造数字/未知候选不修补、其他服务商及非 CTR 请求不重试。未修改诊断、业务指标或 UI。
- 达到本次 AI 功能冻结条件；这是一轮验收结果，不代表服务商未来永不失败，运行时保留校验与 fallback。
